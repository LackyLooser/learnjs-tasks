// Объекты-константы?
// Можно ли изменить объект, объявленный с помощью const? Как вы думаете?

const user = {
  name: "John"
};
// Можно менять свойства обьекта но нельзя поменять сам обьект ( user = {})

// это будет работать?
user.name = "Pete";

// Да это будет работать
